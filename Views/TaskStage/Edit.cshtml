@using IMRepo.Models.Domain;
@model TaskStage
@{
    ViewData["ModelId"] = Model.Id;
    ViewData["DeleteAvailable"] = true;
    ViewData["Title"] = string.Format(JaosUITitles.titleEdit, ModelsTexts.TaskStage.title);
    ViewData["Action"] = "Edit";
    ViewData["UpdateAvailable"] = true;
    ViewData["isBasicView"] = true;
}

@await Html.PartialAsync("/Views/TaskStage/_PartialCapture.cshtml", ViewData)
