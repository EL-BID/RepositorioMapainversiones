@using IMRepo.Models.Domain;
@model PaymentStage
@{
    ViewData["Title"] = string.Format(JaosUITitles.titleCreate, ModelsTexts.PaymentStage.title);
    ViewData["Action"] = "Create";
    ViewData["UpdateAvailable"] = true;
    ViewData["isBasicView"] = true;
}

@await Html.PartialAsync("/Views/PaymentStage/_PartialCapture.cshtml", ViewData)
