@using IMRepo.Models.Domain;
@model Agency
@{
    ViewData["ModelId"] = Model.Id;
    ViewData["DeleteAvailable"] = true;
    ViewData["Title"] = string.Format(JaosUITitles.titleEdit, ModelsTexts.Agency.title);
    ViewData["Action"] = "Edit";
    ViewData["UpdateAvailable"] = true;
    ViewData["isBasicView"] = true;
}

@await Html.PartialAsync("/Views/Agency/_PartialCapture.cshtml", ViewData)
