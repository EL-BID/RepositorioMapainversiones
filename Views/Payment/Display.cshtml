@model IMRepo.Models.Domain.Payment
@{
    ViewData["Title"] = string.Format(JaosUITitles.titleDisplay, ModelsTexts.Payment.title);
    ViewData["UpdateAvailable"] = false;
    ViewBag.WorkingInProject = true;
    ViewData["isBasicView"] = true;
}


@await Html.PartialAsync("/Views/Payment/_PartialCapture.cshtml", ViewData)
