@using IMRepo.Models.Domain;
@model ProjectImage
@{
    ViewData["ModelId"] = Model.Id;
    ViewData["DeleteAvailable"] = true;
    ViewData["Title"] = string.Format(JaosUITitles.titleEdit, ModelsTexts.ProjectImage.title);
    ViewData["Action"] = "Edit";
    ViewData["UpdateAvailable"] = true;
    ViewBag.WorkingInProject = true;
    ViewData["isBasicView"] = true;
}

@await Html.PartialAsync("/Views/ProjectImage/_PartialCapture.cshtml", ViewData)
